/* ============================================================
   终章流程状态（无依赖，避免循环引用）
   mode: tunnel（找照片谜题）→ flyout（指数冲出+白场）→ room（橡木房间）→ done
============================================================ */
export const flow = { mode: 'tunnel' };
